# need to use fixed id so the total length of related, generated codes does not
# exceed the maximum allowed

one:
  id:             101
  l_group:        group_one
  p_group:        group_two
  title:          'Test Interface'
  desc:           'This is a test description of the Test Interface'
  if_level:       0
  if_status:      0
  cfr_record:     one 
  safety_related: false
  note:           'some remark'
