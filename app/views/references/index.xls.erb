<%= CSV.generate_line([
    :code,
    :description,
    :project_doc_id], STD_CSV_OPTIONS ) -%>
<%- @references.each do |r| -%>
<%= CSV.generate_line([
    r.code,
    r.description,
    r.project_doc_id], STD_CSV_OPTIONS ).html_safe -%>
<%- end -%>