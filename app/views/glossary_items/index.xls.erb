<%= CSV.generate_line([
    :term,
    :code,
    :description,
    :cfr_record_id ], STD_CSV_OPTIONS ) -%>
<%- @glossary_items.each do |gi| -%>
<%= CSV.generate_line([
    gi.term,
    gi.code,
    gi.description,
    ( gi.cfr_record.title unless gi.cfr_record_id.nil? )], STD_CSV_OPTIONS ).html_safe -%>
<%- end -%>